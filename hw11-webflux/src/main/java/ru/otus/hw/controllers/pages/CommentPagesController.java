package ru.otus.hw.controllers.pages;

@Deprecated(since = "Используются Functional Endpoint: ru/otus/hw/routers/pages/CommentPagesRouter.java")
//@Controller
public class CommentPagesController {

 /*   @GetMapping("/books/{bookId}/comments")
    private Mono<String> editPage(@PathVariable long bookId) {
        return Mono.just("/comment/show");
    }

    @GetMapping("/books/{bookId}/comments/{id}")
    private Mono<String> editPage(@PathVariable long bookId,
                            @PathVariable long id) {
        return Mono.just("/comment/show");
    }*/
}
